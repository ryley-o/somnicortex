# @somnicortex/runtime

TypeScript coordination runtime for SomniCortex.

## CLI

- `somnicortex-create-agent`
- `somnicortex-send-task`
- `somnicortex-inspect-agent`
- `somnicortex-approve`
- `somnicortex-bootstrap-kernel`
